# 安装ZSH
## 使用自动化脚本来帮助你安装zsh以及zsh配置
主要为ubuntu/debian来安装
其余系统可以修改`auto_install.sh`脚本上2和3行来安装，毕竟那两行只是用来安装zsh而已，后面的都一样，你可以删掉或者改成你系统安装zsh的方法。
**确保脚本文件在你终端的路径上**
运行:
```bash
chmod +x auto_install.sh
./auto_install.sh 
```